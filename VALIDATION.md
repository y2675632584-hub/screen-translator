# 验证记录

环境：macOS 26.4.1，Apple Silicon，Swift 6.3.3。

## 1.3.0 可选原文与目标语言

- 设置新增“原文语言”和“翻译成”两个独立选择框；原文语言包含自动识别，目标语言来自本机 Apple Translation 支持列表。
- 自动模式逐段判断语言并跳过已经是目标语言的文字；手动模式固定 OCR 与翻译源语言。源语言和目标语言相同时会显示提示并阻止开启。
- 语言包状态、下载配置、翻译会话和菜单栏语言摘要均随当前语言组合更新；切换语言会取消旧任务和旧会话，避免缓存译文串组。
- 本机界面验证显示 20 种可识别原文语言（另含自动识别）和 21 种目标语言；目标列表额外包含印地语。
- Release 构建、严格签名和安装版本 1.3.0（build 9）验证通过。6 项快捷键／采集测试与 9 项非系统服务识别测试通过，其中新增自动识别、跳过目标语言及同语言拦截测试。
- 本机临时签名的可执行文件更新后，系统里的旧屏幕录制记录仍显示开启但实际失效。已仅重置 `local.yyy.ScreenTranslator` 的旧记录，通过系统“添加”按钮重新选择 `/Applications/屏幕译.app`，退出并重新打开后应用显示“屏幕录制已授权”；实际开启实时翻译成功读取屏幕，随后关闭并恢复待机。

## 1.2.1 实时翻译防闪烁

- 移除画面一变化就立即清空覆盖层的行为；旧译文会保留到新结果准备完成，再由新结果一次性替换。
- 覆盖窗口已显示时不再每轮重复执行置顶操作，相同翻译结果也不会触发无效重绘。
- 画面指纹保留 320 像素灰度信息并按差异比例判断；忽略时钟、插入点等极小动画，网页滚动、窗口移动和正文变化仍会触发刷新。
- 新增微小动画忽略测试，识别、变化检测、快捷键与采集测试共 15 项通过；本轮系统翻译集成测试因语言包状态暂时未返回“已安装”而跳过 3 项，前一版已在同一系统中验证两种翻译策略正常。
- 已安装 `/Applications/屏幕译.app` 1.2.1（build 8），严格签名校验通过；重新加入屏幕录制列表并按系统提示重启后，应用显示“屏幕录制已授权”。
- 实际切换到实时模式，在英文长页面识别并显示 24 处翻译；滚动一页后持续刷新，无权限错误或语言误判，随后关闭译文并恢复原页面位置。应用现保持实时模式待机。

## 1.2.0 速度与准确度优化

- 自动识别改为严格英语优先 OCR，拉丁字母不再依赖短文本语言猜测；实际全屏测试发现并修复了极短 OCR 字符仍被误判为波兰语的问题，其他语言改由用户手动选择。
- 相邻正文行会合并成带上下文的段落后再翻译；短按钮和不同列内容保持分开，避免界面元素错误拼接。
- 实时模式使用低延迟 OCR、最长边 2200 像素的采集画面和系统低延迟翻译策略；单次模式保留高清 OCR，并请求 macOS 26.4 高质量翻译策略。
- 翻译会话可跨轮次复用；画面变更检测改为 320 像素灰度指纹，避免每 0.8 秒散列整张 Retina 截图。
- 本机检测结果为 Apple Intelligence 语言模型不可用，因此高质量策略由系统回退到传统离线模型；未安装额外大模型，避免在 8 GB 内存设备上造成更慢的响应。
- 17 项测试全部通过，包括真实 Vision OCR、段落合并、画面指纹、低延迟与高质量两种系统翻译策略，以及快捷键和采集取消行为。

## 1.1.3 原生图标更新

- 最终图标采用海玻璃绿／海洋蓝实体色，增加玻璃表面高光、边缘折射和圆润层次；移除中心白色菱形，保留满幅不透明背景。
- 使用 Xcode 26 Icon Composer 创建 `AppIcon.icon`，关闭额外透明效果，再由 `actool` 生成 `Assets.car` 和 `AppIcon.icns`；Info.plist 同时声明 `CFBundleIconName`。
- Release 构建、严格签名、Info.plist、原生图标资源和 10 个标准兼容尺寸验证通过。
- 已更新 `/Applications/屏幕译.app` 至 1.1.3（build 5），注销同标识测试副本并刷新访达缓存；访达截图确认显示新玻璃图标，原先的“灰色底板中再套一个小图标”效果已经消失。
- 应用设置窗口截图确认新版图标生效；用户原有快捷键 `、`、单次翻译模式和语言包状态均保留。
- 系统设置曾显示旧版本授权已开启，但 1.1.3 的 `CGPreflightScreenCaptureAccess()` 返回未授权。已只重置 `local.yyy.ScreenTranslator` 的旧 ScreenCapture 记录，让当前 `/Applications/屏幕译.app` 重新申请，开启后按系统提示退出并重新打开。
- 重启后设置窗口显示「屏幕录制已授权」。实际开启单次翻译后成功读取并识别屏幕内容，返回缺少波兰语语言包的业务状态，证明 ScreenCaptureKit 捕获已通过；再用用户快捷键 `、` 成功关闭翻译。

## 1.1.1 图标更新

- 采用用户确认的 A／中双卡片设计（AppIcon-v3.png），生成 10 个标准尺寸并打包为 AppIcon.icns。
- Info.plist 图标引用、应用内图标资源一致性、Release 构建和严格签名验证通过。
- 更新应用目录修改时间、重新登记并打开应用后，访达和设置窗口截图均确认显示正式图标；菜单栏沿用适合小尺寸显示的单色符号。
- 图标直接嵌入应用资源；不使用会破坏严格签名校验的 Finder 自定义图标资源分叉。

## 1.1.0 更新验证

- Release 构建和应用本机签名验证通过；新版已打开。
- 15 项测试全部通过、无跳过。在原有 9 项基础上增加了普通单键／多修饰键组合、Esc／方向键／功能键、长按与纯修饰键过滤、旧快捷键迁移和保存、单次只采集一帧、实时重复采集及取消停止测试。
- 实际 UI 验证：点击「切换快捷键」后按 K，显示已设置为 K；再录入 Command + Shift + J，显示已设置为 ⇧⌘J。录入中切离应用会取消并恢复旧快捷键。
- 实际 UI 验证：切换到「单次翻译」后说明和选中状态同步更新；退出并重新打开后，自定义快捷键和单次模式均保留。
- 更新前旧版本已有屏幕录制授权；更换本机临时签名构建后，新版显示需要重新授权。因此新版全屏画面上的两种模式仍需重新授权后验收，采集次数与取消行为已用可注入采集回调的测试验证。

## 已完成

- Debug 编译、Release 构建和 `.app` 本机签名验证通过。
- 9 项 XCTest 全部通过，无跳过：中文和数字跳过、中英混排范围、Retina 点坐标映射、缓存隔离和清理、画面变更指纹、真实 Vision 图片 OCR、真实中英混排图片 OCR、真实系统英译中批量翻译、取消后不再翻译。
- 应用启动成功，菜单栏和首次设置窗口可见，布局经过实际截图检查。
- 系统语言包下载面板正常，英语和简体中文语言包已下载完成；设置显示「已安装，可离线使用」。
- 最终版本已重新打开，确认开机启动默认关闭；当前屏幕录制权限和英语语言包均显示就绪。

## 尚待对应环境验证

- 单次模式的屏幕捕获和快捷键关闭已实测通过；实时滚动跟随和点击穿透仍需在英文网页画面继续验收。
- 真实外接显示器、多屏缩放、全屏应用、权限撤销和断网场景仍需要在对应环境验证。
- 已安装语言包的真实 Translation 输出通过测试；未改变用户网络连接，因此断网情境不标记为已实测。

## 环境注意

受限命令环境无法访问系统 Vision 识别服务时，真实 OCR 测试返回 `nilError`；在获准访问本机服务的运行环境重试后全部通过。未将失败测试跳过。
