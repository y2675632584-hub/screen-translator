# 隐私说明

屏幕译不建立用户账户，也不包含分析、广告或遥测代码。

## macOS

截图、文字识别和翻译均由 macOS 系统框架在设备上处理。应用不保存截图、识别文字或翻译历史，也不会把这些内容发送给本项目维护者。

## Windows

截图和 OCR 文字识别在本机内存中完成，截图不会上传。用户开启翻译后，应用会把 OCR 识别出的文字、所选语言和必要的翻译指令发送给 OpenAI API，以返回译文。请求使用用户自己提供的 API Key，并受 OpenAI 的 API 数据使用政策约束。

API Key 使用 Windows DPAPI 按当前系统账户加密，保存在 `%LocalAppData%\ScreenTranslator`。用户可以在设置中随时移除密钥。

两个版本在关闭翻译后都会停止屏幕采集、取消进行中的任务并清空内存翻译缓存。
