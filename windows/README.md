# 屏幕译 Windows 版

这是免安装便携版。请解压完整文件夹后运行 `ScreenTranslator.exe`，不要只把 exe 单独移动出去。

首次使用：

1. 在设置中填入自己的 OpenAI API Key。
2. 选择“自动识别”或已安装的 OCR 语言，并选择目标语言。
3. 保存后按 F8 开始翻译，再按一次关闭。
4. 双击系统托盘中的图标可再次打开设置。

截图和文字识别在本机完成，截图不会上传；只有 OCR 识别出的文字会发送给 OpenAI。API Key 使用当前 Windows 账户的 DPAPI 加密保存。
